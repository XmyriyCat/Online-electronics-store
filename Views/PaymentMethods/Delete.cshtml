@using AdminPanel.ModelsDb
@model PaymentWay

@{
    Layout = "_AdminLayout";
}

<h2>Delete Payment Method</h2>

<form method="post">
    <div class="form-row">
        <div class="col-md-4 mb-3">
            <label>Payment Method ID</label>
            <input type="text" class="form-control" asp-for="@Model.Id" readonly>
        </div>

        <div class="col-md-4 mb-3">
            <label>Payment Method Name</label>
            <input type="text" class="form-control" asp-for="@Model.Name" readonly>
        </div>

        <button type="submit" asp-controller="PaymentMethods" asp-action="RemovePaymentMethod" class="btn btn-danger">Delete</button>
        <button type="submit" asp-controller="PaymentMethods" asp-action="BackToMenu" class="btn btn-primary">Cancel</button>
    </div>
</form>